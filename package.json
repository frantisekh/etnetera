{
  "name": "etnetera",
  "version": "1.0.0",
  "description": "Etnetera coding task",
  "main": "index.js",
  "scripts": {
    "sass": "sass src/scss:dist/css --watch",
    "serve": "browser-sync start --server 'dist' --files 'dist'",
    "copy:html": "cp src/index.html dist/",
    "copy:js": "cp src/js/* dist/js/",
    "copy": "npm run copy:html && npm run copy:js",
    "start": "npm run copy && run-p sass serve",
    "build": "sass src/scss:dist/css --style=compressed && npm run copy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "npm-run-all": "^4.1.5",
    "sass": "^1.71.1"
  }
}
